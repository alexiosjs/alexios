/**
 * 打包配置
 *
 * @param {object} props
 *
 * @returns {object} webpackConfig
 */

const buildGenerator = (a, b, c) => {
  const TEST = 1;
  const B = 12;
  // TODO: 项目打包配置
};

export default buildGenerator;
